function Column({ text }) {
  return (
    <span className="tailwind.config.jsfont-medium tailwind.config.jstext-sm tailwind.config.jscapitalize">
      {text}
    </span>
  );
}

export default Column;
